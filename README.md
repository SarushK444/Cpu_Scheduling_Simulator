# CPU Scheduling Simulator (Mini OS Project)
A C++ based CPU Scheduling Simulator that demonstrates and compares different scheduling algorithms used in Operating Systems.

The project simulates process execution, generates Gantt charts, and calculates key performance metrics such as Waiting Time, Turnaround Time, and CPU Utilization.
